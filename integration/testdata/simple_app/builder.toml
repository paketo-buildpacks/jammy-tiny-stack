
description = "Tiny jammy image (jammy build image, distroless-like run image) with no buildpacks included."

[lifecycle]
  version = "0.14.0"

[stack]
  build-image = "docker.io/paketobuildpacks/build:1.3.44-tiny-cnb"
  id = "io.paketo.stacks.jammy.tiny"
  run-image = "index.docker.io/paketobuildpacks/run:tiny-cnb"
